import { SHOW_QUERY_LOGS } from "@/lib/constants";

export const getVerificationTokenByEmail = async (email: string) => {
  // Sanity CMS removed - function disabled
  console.log(
    "getVerificationTokenByEmail: Sanity CMS removed, returning null",
  );
  return null;
};

export const getVerificationTokenByToken = async (token: string) => {
  // Sanity CMS removed - function disabled
  console.log(
    "getVerificationTokenByToken: Sanity CMS removed, returning null",
  );
  return null;
};

export const getVerificationTokenByIdentifierAndToken = async (
  identifier: string,
  token: string,
) => {
  // Sanity CMS removed - function disabled
  console.log(
    "getVerificationTokenByIdentifierAndToken: Sanity CMS removed, returning null",
  );
  return null;
};
