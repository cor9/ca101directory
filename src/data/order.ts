import { SHOW_QUERY_LOGS } from "@/lib/constants";

export const getOrderByUserIdAndItemId = async (
  userId: string,
  itemId: string,
) => {
  // Sanity CMS removed - function disabled
  console.log("getOrderByUserIdAndItemId: Sanity CMS removed, returning null");
  return null;
};
