import { SHOW_QUERY_LOGS } from "@/lib/constants";

export const getAccountByUserId = async (userId: string) => {
  // Sanity CMS removed - function disabled
  console.log("getAccountByUserId: Sanity CMS removed, returning null");
  return null;
};

export const getAccountByProviderAccountId = async (
  providerAccountId: string,
  provider: string,
) => {
  // Sanity CMS removed - function disabled
  console.log(
    "getAccountByProviderAccountId: Sanity CMS removed, returning null",
  );
  return null;
};
