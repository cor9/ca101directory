import { SHOW_QUERY_LOGS } from "@/lib/constants";

export const getUserByEmail = async (email: string) => {
  // Sanity CMS removed - function disabled
  console.log("getUserByEmail: Sanity CMS removed, returning null");
  return null;
};

export const getUserById = async (userId: string) => {
  // Sanity CMS removed - function disabled
  console.log("getUserById: Sanity CMS removed, returning null");
  return null;
};

export const getUserByIdWithAccounts = async (userId: string) => {
  // Sanity CMS removed - function disabled
  console.log("getUserByIdWithAccounts: Sanity CMS removed, returning null");
  return null;
};
