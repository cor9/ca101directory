import { urlForImage } from "./image";

/**
 * Portable text to markdown conversion - disabled due to Sanity CMS removal
 */
// biome-ignore lint/suspicious/noExplicitAny: <explanation>
export function portableTextToMarkdown(body: any) {
  // Sanity CMS removed - function disabled
  console.log(
    "portableTextToMarkdown: Sanity CMS removed, returning empty string",
  );
  return "";
}
